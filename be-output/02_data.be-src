
data One
{
	guid;
	autoid;
	created;
	updated;
	deleted;
	key = value;
	list<Four>(1, 2, 3);
}

data Four
{
	map(go: "ready", for: "hell");
	Nine = null;
}

data Nine
{
	timestamp begin;
	timestamp close;
	enum group;
	int sum = 0;
}

new One();

select
create_or_update
delete

select * from One where list contains(1, 2) limit 10;
select One, Four, Nine 
